{
  "name": "common",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "src/frontend/common/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint src/frontend/common --deny-warnings"
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxfmt --write src/frontend/common"
      }
    },
    "format:check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxfmt --check src/frontend/common"
      }
    }
  }
}
